define [
    "wire!bootstrap/spec"
], ->
    # all specs must be in folder defined in SpecRunner.pathToSpec
    specs = [
        # "applicationCore"
        "modules/profiles"
        "plugins/apply"
    ]
