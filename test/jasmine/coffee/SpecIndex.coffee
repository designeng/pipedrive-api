define [
    "wire!bootstrap/spec"
], ->
    # all specs must be in folder defined in SpecRunner.pathToSpec
    specs = [
        "modules/profiles"
        "plugins/apply"
        "plugins/sandbox"
    ]
